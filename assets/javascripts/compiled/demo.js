// Generated by CoffeeScript 1.4.0
(function() {

  $(function() {
    var attrs;
    attrs = {
      x: {
        caption: "Last 400 data points"
      },
      y: {
        caption: "Response time, seconds",
        buffer_size: 400
      }
    };
    Eventoverse.canvas = new Eventoverse.Graphs.Canvas("#line_chart", attrs);
    Eventoverse.canvas.addElement(Eventoverse.Graphs.Histogram);
    return Eventoverse.canvas.render(Eventoverse.RandomData.generate(20));
  });

  this.Eventoverse.RandomData = (function() {

    function RandomData() {}

    RandomData.counter = 100;

    RandomData.generate_one = function() {
      var rnd, time;
      time = Math.floor(new Date().getTime() / 1000);
      rnd = Math.random() * 10;
      Eventoverse.RandomData.counter = Eventoverse.RandomData.counter + 20;
      return {
        x: time + Eventoverse.RandomData.counter,
        y: rnd
      };
    };

    RandomData.generate = function(num) {
      var all;
      all = _.map(_.range(0, num), this.generate_one);
      return {
        values: all,
        key: "Test Data",
        color: "#123456"
      };
    };

    return RandomData;

  })();

}).call(this);
